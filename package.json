{
  "name": "certifiedpass",
  "version": "0.1.0",
  "private": true,
  "description": "CertifiedPass — AI-powered verifiable credential platform for real-world achievements",
  "engines": {
    "node": ">=22.13.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean && rimraf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,css,sol}\"",
    "db:generate": "pnpm --filter @certifiedpass/api exec prisma generate",
    "db:migrate": "pnpm --filter @certifiedpass/api exec prisma migrate dev",
    "db:seed": "pnpm --filter @certifiedpass/api exec prisma db seed",
    "db:studio": "pnpm --filter @certifiedpass/api exec prisma studio",
    "contracts:build": "pnpm --filter @certifiedpass/contracts build",
    "contracts:test": "pnpm --filter @certifiedpass/contracts test"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-solidity": "^1.4.1",
    "rimraf": "^6.0.1",
    "turbo": "^2.1.3",
    "typescript": "^5.6.2"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "contracts"
  ]
}

